{
  "name": "masterly-deploy",
  "version": "1.0.0",
  "description": "Masterly Learning Platform",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "graphlib": "^2.1.8"
  }
}
